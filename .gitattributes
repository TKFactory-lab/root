# .gitattributes - Git LFS tracking rules for large binaries and native extensions
# Added 2025-08-24 by automation to avoid large binary check-ins.
# Patterns chosen from large-files.csv analysis (e.g. *.so, lib*.so.*, native bins in modules/**/bin)

# Native Python/CPython extension modules
*.so filter=lfs diff=lfs merge=lfs -text
*.abi3.so filter=lfs diff=lfs merge=lfs -text
*.cpython-*-*.so filter=lfs diff=lfs merge=lfs -text
# Shared libraries with versioned suffixes (e.g. libarrow.so.2100)
lib*.so* filter=lfs diff=lfs merge=lfs -text
# Generic dynamic libraries / platform binaries
*.pyd filter=lfs diff=lfs merge=lfs -text
*.dll filter=lfs diff=lfs merge=lfs -text
*.dylib filter=lfs diff=lfs merge=lfs -text
# Native helper binaries often bundled under modules/**/bin
modules/**/bin/* filter=lfs diff=lfs merge=lfs -text
# PyPI native wheels/lib directories (catch-alls)
modules/**/native/** filter=lfs diff=lfs merge=lfs -text
# Modules native shared libs
modules/**/*.so filter=lfs diff=lfs merge=lfs -text
# Ensure consistent line endings and avoid LF->CRLF warnings on Windows
* text=auto eol=lf
# Treat virtual envs and site-packages as binary (no line-ending conversion)
final_crewai_env/** -text
final_crewai_env_corrupt*/** -text
venv/** -text
env/** -text
backups/** filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text
*.tar.gz filter=lfs diff=lfs merge=lfs -text
